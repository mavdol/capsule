import path from 'path-browserify';

export default path;
export const {
    resolve,
    join,
    dirname,
    basename,
    extname,
    format,
    parse,
    sep,
    delimiter,
    posix,
    win32
} = path;
